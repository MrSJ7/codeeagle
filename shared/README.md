# Shared Module

This directory is reserved for data structures, constants, and schema definitions that are conceptually shared between the client and the server (such as review finding schemas, severity levels, and category definitions).
